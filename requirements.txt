# Python dependencies for the audit + analysis pipeline.
#
# Version floors are pinned for reproducibility (a DOI'd benchmark must not silently
# change behavior when a transitive dep updates). For an EXACT reproduction, generate
# a lockfile from a known-good environment: `pip freeze > requirements.lock`.

# --- Core pipeline ---
openai>=1.30            # OpenRouter-compatible client (model calls + LLM judge)
httpx>=0.27             # Dynamic model resolution from the OpenRouter API
requests>=2.31
tenacity>=8.2           # Auto-retry transient API failures (see audit_runner)
python-dotenv>=1.0      # .env loading
pydantic-settings>=2.0  # Environment validation
sqlalchemy>=2.0         # SQLite result store
tqdm>=4.66

# --- Data + statistics (affect published results — pin conservatively) ---
pandas>=2.0
numpy>=1.24
scipy>=1.10             # McNemar chi-square, Cramér's V
scikit-learn>=1.3       # Cohen's Kappa
statsmodels>=0.14       # Power analysis / MDES
tabulate>=0.9           # pandas to_markdown

# --- Visualization (offline report charts) ---
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18

# --- Semantic clustering ---
sentence-transformers>=2.6

# --- Background jobs (optional advanced pipeline) ---
celery>=5.3
redis>=5.0

# --- Dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
black>=24.0

# NOTE: streamlit, google-generativeai, psycopg2-binary and dvc were removed — the
# frontend is Next.js (web/), all model calls go through the OpenRouter
# (openai-compatible) client, and none of those packages are imported anywhere.
